{
  "scripts": {
  "build": "rm -rf node_modules && rm package-lock.json && npm install"
},

  "dependencies": {
    "axios": "^1.8.4",
    "canvas": "^3.1.0",
    "dotenv": "^16.5.0",
    "qrcode": "^1.5.4",
    "telegraf": "^4.16.3"
  }
}
